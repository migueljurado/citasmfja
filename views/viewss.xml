<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="citasmja.list">
      <field name="name">citasmja list</field>
      <field name="model">citasmja.citasmja</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="citasmja.action_window">
      <field name="name">citasmja window</field>
      <field name="res_model">citasmja.citasmja</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="citasmja.action_server">
      <field name="name">citasmja server</field>
      <field name="model_id" ref="model_citasmja_citasmja"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="citasmja" id="citasmja.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="citasmja.menu_1" parent="citasmja.menu_root"/>
    <menuitem name="Menu 2" id="citasmja.menu_2" parent="citasmja.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="citasmja.menu_1_list" parent="citasmja.menu_1"
              action="citasmja.action_window"/>
    <menuitem name="Server to list" id="citasmja" parent="citasmja.menu_2"
              action="citasmja.action_server"/>
    -->
  </data>
</odoo>